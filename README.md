# T1 - Textura

Instale as dependências:

```bash
pip install -r requirements.txt
```

Converta todas as imagens de um diretório e salve o resultado em outro:

```bash
python main.py images --output-dir grayscale
```

Para incluir imagens dentro de subdiretórios, use `--recursive`.
